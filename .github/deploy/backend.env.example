# Copy this file's contents into the GitHub secret named BACKEND_ENV_FILE.
# Keep the format exactly like a normal backend/.env file.

TYPE=ANT
URL=https://api.opusmax.pro
MODEL=claude-sonnet-4-6
KEY=replace-with-your-provider-key
SEMANTIC_SCHOLAR_API_KEY=replace-with-your-semantic-scholar-key
